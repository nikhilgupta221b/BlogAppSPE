---
- name: Deploy Blog Application
  hosts: local
  become: false
  tasks:
    - name: Pull and Run Docker Compose
      community.docker.docker_compose_v2:
        project_src: "{{ playbook_dir }}"
        state: present
        pull: 'missing'  # Ensure this is set correctly
        restarted: yes
